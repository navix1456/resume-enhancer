export const environment = {
  production: false,
  GEMINI_API_KEY: 'YOUR_GEMINI_API_KEY_LOCAL', // IMPORTANT: Replace with your actual key in .env for local development, or your Vercel env variable for deployment.
  apiUrl: 'http://localhost:3000',
  // Add other non-sensitive configuration
};